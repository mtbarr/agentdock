{
  "path": "bundle/gemini.js",
  "find": "async function main() {\n  const cliStartupHandle = startupProfiler.start(\"cli_startup\");",
  "replace": "async function outputUsageJson(config) {\n  try {\n    const authType = config.getContentGeneratorConfig()?.authType;\n    if (authType !== \"oauth-personal\" && authType !== \"compute-default-credentials\") {\n      return;\n    }\n    const { CodeAssistServer, UserAccountManager, setupUser } = await import(\"./chunk-2OFO4ODK.js\");\n    const authClient = await getOauthClient(authType, config);\n    const userData = await setupUser(authClient, config, {});\n    const caServer = new CodeAssistServer(\n      authClient,\n      userData.projectId,\n      {},\n      \"\",\n      userData.userTier,\n      userData.userTierName,\n      userData.paidTier,\n      config\n    );\n    const quota = await caServer.retrieveUserQuota({\n      project: userData.projectId\n    });\n    const email = new UserAccountManager().getCachedGoogleAccount();\n    process.stdout.write(\"__GEMINI_USAGE_JSON__\" + JSON.stringify({ email, userTierName: userData.userTierName, quota }) + \"\\n\");\n  } catch {\n  }\n  await runExitCleanup();\n  process.exit(0);\n}\nasync function main() {\n  const cliStartupHandle = startupProfiler.start(\"cli_startup\");"
}
